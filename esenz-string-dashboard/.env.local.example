# eSenZ plant API key.
#
# NEVER commit the real value and never expose it to the browser — it is only
# read server-side by pages/api/esenz-live.js via process.env.
#
# Usage: copy this file to `.env.local` and replace with your plant key.
ESENZ_KEY=your_key_here
